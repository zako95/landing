name: GitHub Pages deploy

on:
  push:
    branches: [master, feature/multilang]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
      - name: Use Node.js 16.x
        uses: actions/setup-node@v1
        with:
          node-version: "16.x"
          
      - name: Cache translations
        uses: actions/cache@v2
        with:
          path: ./.cache
          key: translation-cache
          
      - name: Install
        run: npm ci

      - name: Log in to the Container registry
        uses: docker/login-action@f054a8b539a109f9f41c372932f1ae047eff08c9
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}

      - name: Build and push Docker image
        uses: docker/build-push-action@ad44023a93711e3deb337508980b4b5e9bcdc5dc
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            translation_key=${{ secrets.NEXT_TRANSLATION_API_KEY }}

#
#      - name: Build
#        run: npm run build && npm run export && touch ./out/.nojekyll
#        env:
#          NEXT_TRANSLATION_API_KEY: ${{ secrets.NEXT_TRANSLATION_API_KEY }}
#
#      - name: Deploy 🚀
#        uses: JamesIves/github-pages-deploy-action@4.1.0
#        with:
#          TOKEN: ${{ secrets.ACCESS_TOKEN }}
#          BRANCH: publish # The branch the action should deploy to.
#          FOLDER: out # The folder the action should deploy.